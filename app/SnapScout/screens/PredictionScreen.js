import { View, Text } from "react-native";

export default PredictionScreen = ({ navigation }) => {
    return (
        <View>
            <Text>predictions from the ML model</Text>
        </View>
    )
} 