import { View, Text } from "react-native";

export default FormScreen = ({ navigation }) => {
    return (
        <View>
            <Text>form for user to correct CV errors</Text>
        </View>
    )
}